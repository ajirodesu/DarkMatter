import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import Quickshell
import qs.Common
import qs.Modules.Settings
import qs.Services
import qs.Widgets

Item {
    id: themeColorsTab

    property var parentModal: null

    // This file is now just a placeholder - all content has been moved to sub-tabs
    // Displays section is now in DisplaysTab.qml
                                Item {
                                    anchors.fill: parent
    }
}
